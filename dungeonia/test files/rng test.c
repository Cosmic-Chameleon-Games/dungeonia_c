#include <stdio.h>
#include <stdlib.h>

int main (){

    int lvl;
    int exp = 135;
    int rng;

    srand(time(NULL));

        rng = rand()%  2;

    printf("%d", rng);

return (0);
}

